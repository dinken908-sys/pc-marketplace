meta:
  environment: nodejs
  toolchain:
    name: npm
    version: "20"
build:
  skip: false
  additionalCommands: 
    - cd backend && npm install
  artifacts:
    "*": /
run:
  scriptName: null
  scriptArguments: null
  nodeArguments: null
  command: cd backend && npm start
  persistenceMount: /data
  containerPort: "80"
  servicePort: "80"